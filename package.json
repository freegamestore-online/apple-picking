{
  "name": "apple-picking",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @apple-picking/web dev",
    "build": "pnpm --filter @apple-picking/web build",
    "preview": "pnpm --filter @apple-picking/web preview",
    "typecheck": "pnpm --filter @apple-picking/web exec tsc -b",
    "test": "pnpm --filter @apple-picking/web test"
  }
}
